#include <iostream>
#include <vector>

using namespace std;

int main() {
    bool debug = true;
    if (debug) {
        freopen("input.in", "r", stdin);
    } else {
        freopen("input.in", "r", stdin);
        freopen("input.out", "w", stdout);
    }
}
