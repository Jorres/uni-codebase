#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios::sync_with_stdio(false);

    int k, n;
    cin >> k >> n;
    vector<int> xs;
    vector<int> ys;

    for (int i = 0; i < n; i++) {
        int x, y; 
        cin >> x >> y;
        xs.push_back(x);
        ys.push_back(y);
    }

    long long inner = 0;
    long long outer = 0;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            
        }
    }
}
